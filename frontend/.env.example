# Google OAuth Client ID (leave empty for dev mode)
VITE_GOOGLE_CLIENT_ID=

# WebSocket URL (defaults to ws://localhost:8000/ws in dev)
VITE_WS_URL=ws://localhost:8000/ws

# API URL (defaults to /api with Vite proxy in dev)
VITE_API_URL=
