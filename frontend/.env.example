# Google OAuth Client ID (public — safe for frontend code)
# Leave empty to hide the Google sign-in button and only show Dev Mode.
VITE_GOOGLE_CLIENT_ID=

# WebSocket URL (defaults to ws://127.0.0.1:8000/ws in dev)
VITE_WS_URL=

# API URL (defaults to empty, uses Vite proxy /api in dev)
VITE_API_URL=
